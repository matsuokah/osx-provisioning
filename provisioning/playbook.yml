---
- name: OSX Local Provisioner
  hosts: localhost
  sudo: no
  roles:
    # App Installations
    - common
    - brew
    - brew-cask

    # Configurations
    - git
    - vim
    - xcode
    - zsh
#    - defaults
